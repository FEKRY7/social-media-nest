export enum UserType {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum PrivacyType {
  PUBLIC = 'Public',
  ONLYME = 'Only Me',
  PRIVATE = 'Private',
}

export enum StatusType {
  ONLINE = 'Online',
  OFFLINE = 'Offline',
  BLOCKED = 'Blocked',
  SOFTDELETED = 'SoftDeleted',
}
 